## Tasks

### Setup

- [x] T-0: Create branch `feature/coc-admin-escalation` in code repo (`finding-a-bed-tonight`) from main. **Session 1 (2026-04-10).**
- [x] T-1: Capture pre-change baseline. Tee to `logs/coc-escalation-baseline-pre.log`. Result: 38 tests green across `ReferralEscalationIntegrationTest` (3), `NotificationDiagnosticTest` (2), `NotificationPaginationTest` (6), `NotificationRlsIntegrationTest` (12), `SseNotificationIntegrationTest` (11), `SseStabilityTest` (4). **Session 1 (2026-04-10).** *(Playwright baseline deferred to a frontend session — backend-only chunk doesn't risk Playwright regressions yet.)*

### Backend — Schema (Flyway)

- [x] T-2: **V46 (renumbered from V40 post-v0.34.0) — `escalation_policy` table.** Created as `backend/src/main/resources/db/migration/V46__create_escalation_policy.sql`. Append-only versioned per (tenant_id, event_type). UUID PK, tenant_id FK NULL (platform default), event_type VARCHAR(64), version INTEGER, thresholds JSONB, created_at, created_by FK with `ON DELETE SET NULL` (preserves audit-trail integrity if admin removed), `UNIQUE NULLS NOT DISTINCT (tenant_id, event_type, version)` to prevent duplicate platform-default rows. Index `idx_escalation_policy_current (tenant_id, event_type, version DESC)`. RLS enabled with `USING (true)` SELECT/INSERT, no UPDATE/DELETE policies (append-only enforced at DB layer). **Session 1 (2026-04-10)**, with Elena Vasquez round-table fixes F1 (NULLS NOT DISTINCT) and F2 (ON DELETE SET NULL). (D7)
- [x] T-3: **V46 seed — platform default policy.** Inserted as part of V46 migration. tenant_id=NULL, event_type='dv-referral', version=1, thresholds match the existing hardcoded values exactly: `[{PT1H, ACTION_REQUIRED, [COORDINATOR]}, {PT2H, CRITICAL, [COC_ADMIN]}, {PT3H30M, CRITICAL, [COORDINATOR, OUTREACH_WORKER]}, {PT4H, INFO, [OUTREACH_WORKER]}]`. **Session 1 (2026-04-10).** (D7)
- [x] T-4: **V47 (renumbered from V41) — `referral_token.escalation_policy_id` column.** Added in `backend/src/main/resources/db/migration/V47__referral_token_admin_columns.sql`. Nullable UUID FK to escalation_policy(id). Existing rows NULL → batch job falls back to platform default policy. Backwards compatible by design. **Session 1 (2026-04-10).** (D7)
- [x] T-5: **V47 — `referral_token.claimed_by_admin_id` and `claim_expires_at` columns.** Added in V47. Both nullable. Two new partial indexes: `idx_referral_token_active_claim (claim_expires_at) WHERE claimed_by_admin_id IS NOT NULL` for the auto-release scheduler, and `idx_referral_token_pending_by_expiry (tenant_id, expires_at) WHERE status = 'PENDING'` for the admin queue endpoint (Session 3). **Session 1 (2026-04-10).** (D4) *(EXPLAIN ANALYZE verification at scale deferred to Session 3 per Sam Okafor's note about RLS+index interaction.)*
- [x] T-6: **Audit event types (no SQL migration at this step).** **SPEC DRIFT NOTE:** the spec assumed an `AuditEventType` enum existed. It does not — the codebase uses bare String literals (e.g. `"ROLE_CHANGED"`) passed to `AuditEventService`. To stay additive, Session 1 created `backend/src/main/java/org/fabt/shared/audit/AuditEventTypes.java` as a `public final class` with 6 `public static final String` constants: `DV_REFERRAL_CLAIMED`, `DV_REFERRAL_RELEASED`, `DV_REFERRAL_REASSIGNED`, `DV_REFERRAL_ADMIN_ACCEPTED`, `DV_REFERRAL_ADMIN_REJECTED`, `ESCALATION_POLICY_UPDATED`. NO refactor of existing String literals (deferred to a future cleanup change). **Post-v0.34.0 rebase update:** v0.34.0 bed-hold-integrity also created `AuditEventTypes.java` (independently) to hold `BED_HOLDS_RECONCILED`. During the post-v0.34.0 rebase the two add/add conflicts were resolved by unioning the constant sets into a single file containing all 7 constants (and a 7-test `AuditEventTypesTest` contract pin). The audit_events.actor_user_id nullable schema change is now V48 (see T-16 discovery). **Session 1 (2026-04-10).** (D8)

### Backend — Escalation Policy Service

- [x] T-7: `EscalationPolicy` domain object (record) + `EscalationPolicyRepository` (Spring Data JDBC). Read methods: `findCurrentByTenantAndEventType(tenantId, eventType)`, `findById(id)`. Write methods: `insertNewVersion(...)`. No update or delete methods — append-only. (D7) **Session 2 (2026-04-10).**
- [x] T-8: `EscalationPolicyService` with two Caffeine caches:
  - `currentPolicyByTenant` — key=(tenantId, eventType), value=policy, max=200, TTL=5min — used by `ReferralTokenService.create()` to snapshot the policy on each new referral.
  - `policyById` — key=policyId, value=policy, max=500, TTL=10min — used by the batch job to look up the frozen policy for each referral.
  - Both caches invalidated programmatically on PATCH. (D7) **Session 2 (2026-04-10).**
  - *Persona — Sam Okafor:* "5-min TTL is fine because policies change rarely. Don't query the DB on every batch run."
- [x] T-9: Validation in `EscalationPolicyService.update(...)`: monotonically-increasing thresholds (`Duration.parse(t[i].at).compareTo(t[i+1].at) < 0`), valid roles in recipients (`COORDINATOR|COC_ADMIN|OUTREACH_WORKER|PLATFORM_ADMIN`), valid severities (`INFO|ACTION_REQUIRED|CRITICAL`). Reject invalid policies with 400 + structured error. (D7) **Session 2 (2026-04-10).**
  - *Persona — Riley Cho:* "Validation must happen at the service layer, not just the controller, so direct service calls (e.g. tests) can't bypass it."

### Backend — Refactor Escalation Job

- [x] T-10: Refactor `ReferralEscalationJobConfig.run()` to read thresholds from the **frozen** policy attached to each pending referral (`escalation_policy_id` FK lookup via `EscalationPolicyService.findById(...)`). Fall back to platform default if `escalation_policy_id IS NULL` (existing rows). (D7) **Session 2 (2026-04-10).**
  - *Architecture Note — Alex Chen:* "Job moved to `org.fabt.referral.batch` to break the circular dependency between modules."
  - *Performance Note — Sam Okafor:* "Loop uses local caching for policies and recipient lists to eliminate N+1 hits."
- [x] T-11: Refactor `ReferralTokenService.create(...)` to call `EscalationPolicyService.getCurrentForTenant(tenantId, "dv-referral")` and snapshot the resulting `policy.id` into the new referral's `escalation_policy_id` column. (D7) **Session 2 (2026-04-10).**
- [x] T-12: Refactor recipient resolution in the batch job to read the recipient roles from the policy, not hardcoded role names. Each threshold can target one or more roles; resolve to user list via `userService.findActiveByRoles(tenantId, roles)`. Preserve the existing dedup pattern (`existsByTypeAndReferralId`). (D7) **Session 2 (2026-04-10).**



### Backend — Admin Endpoints

- [x] T-13: `GET /api/v1/dv-referrals/escalated` — returns pending DV referrals scoped to the caller's tenant. **Session 3 (2026-04-10).** Cross-tenant filter at the SQL layer (`WHERE tenant_id = ?`) — referral_token RLS only checks dvAccess, so the service layer applies the tenant filter; this also lets the planner use the V47 partial index `idx_referral_token_pending_by_expiry` (renumbered from V41 during the post-v0.34.0 rebase). Coordinator/admin name lookups go through `userService.findDisplayNamesByIds(...)` returning `Map<UUID, String>` so the referral module never imports `auth.domain.User` (Alex Chen ArchUnit boundary). (D3)
  - *Persona — Marcus Webb:* "Verify the response shape includes zero PII before merge. The DTO is the contract." Verified by `EscalatedQueueEndpointTest.zeroPiiInResponse`.
- [x] T-14: `POST /api/v1/dv-referrals/{id}/claim` — atomic conditional UPDATE (`tryClaim` uses `UPDATE ... RETURNING *` — single round-trip, closes the TOCTOU window where two admins both pass a Java-level read check and both write). Returns 200 with the updated DTO, or 409 if already claimed and neither `override=true` query param nor `Override-Claim: true` header is set. Writes audit event `DV_REFERRAL_CLAIMED` via `ApplicationEventPublisher` → `AuditEventRecord` → `AuditEventService` listener (matching the existing `UserService.publishAuditEvent` pattern). Publishes SSE `referral.claimed`. Claim duration is configurable via `fabt.dv-referral.claim-duration-minutes` (default 10). **Session 3 (2026-04-10).** (D4, D8, D9)
- [x] T-15: `POST /api/v1/dv-referrals/{id}/release` — atomic conditional UPDATE via `ReferralTokenRepository.tryRelease`. Only the claiming admin can release; other admins must pass `override=true` or `Override-Claim: true`. Idempotent on no-op (claim already released, e.g. auto-released between page load and click). Writes audit `DV_REFERRAL_RELEASED` with `reason: "manual"`. Publishes SSE `referral.released`. **Session 3 (2026-04-10).** (D4, D8, D9)
- [x] T-16: `@Scheduled(fixedRate = 60_000)` auto-release task in `ReferralTokenService.autoReleaseClaims()`. The query `clearExpiredClaims` includes `WHERE claimed_by_admin_id IS NOT NULL AND claim_expires_at < NOW()` so the V47 partial index `idx_referral_token_active_claim` is actually used (Sam Okafor catch). Returns `(id, tenant_id)` pairs so per-claim SSE events are routed to the correct tenant scope (the `@Scheduled` thread inherits no tenant). Defense-in-depth: throws if dvAccess is missing (matches `expireTokens` pattern). Writes one `DV_REFERRAL_RELEASED` audit row per release with `actor_user_id = NULL`, `reason: "timeout"`. **Session 3 (2026-04-10).** Required new migration **V48** (renumbered from V42 post-v0.34.0 rebase) to make `audit_events.actor_user_id` nullable — without this, system audit rows were silently dropped by the listener's try/catch. On v0.34.0+ deployments V48 is a no-op because V44 (bed-hold-integrity) already dropped the constraint; preserved for lineage per Elena Vasquez. ShedLock TODO comment added per project convention. (D4)
- [x] T-17: `POST /api/v1/dv-referrals/{id}/reassign` — body `{targetType, targetUserId?, reason}`. **Session 4 (2026-04-10).** Three target types: `COORDINATOR_GROUP` fans out via `CoordinatorAssignmentRepository.findUserIdsByShelterId`; `COC_ADMIN_GROUP` fans out via `userService.findActiveUserIdsByRole`; `SPECIFIC_USER` notifies only the named user AND sets `escalation_chain_broken=true` (V49 column, renumbered from V43 post-v0.34.0) so the batch tasklet skips the referral. **Cross-tenant guard** at the service layer (round-3 BLOCKER): asserts `token.tenantId == TenantContext.getTenantId()` because referral_token RLS only checks dvAccess. **Cross-tenant target user check** via `userService.existsByIdInCurrentTenant` (boundary-clean primitive). **Chain-resume** (round-4 BLOCKER, Marcus Okafor): COORDINATOR_GROUP and COC_ADMIN_GROUP reassigns clear the chain-broken flag via `markEscalationChainResumed` so an admin can "give back" a referral after a personal reassign. **Notification payload PII reduction** (Keisha Thompson round 3): the admin's free-text reason lives ONLY in the audit row, never in the broadcast payload — recipients see `referralId + targetType` only. **Audit details enriched** (Casey Drummond rounds 3+5): `targetType`, `targetUserId?`, `reason`, `recipientCount`, `shelterId` (single-table subpoena queries), `actorRoles` (frozen-at-action), `chainResumed:true` when applicable. **Zero-recipient WARN log** (Marcus Okafor round 3) when group fan-out paged nobody. Writes `DV_REFERRAL_REASSIGNED`. Publishes SSE `referral.queue-changed`. (D5, D8, D9)
- [x] T-18: `GET /api/v1/admin/escalation-policy/{eventType}` — returns the current policy for the caller's tenant; falls back to platform default. Response is `EscalationPolicyDto(id, tenantId, eventType, version, thresholds, createdAt, createdBy)` with `non_null` Jackson inclusion (null `tenantId`/`createdBy` are omitted from JSON, not serialized as `:null`). New controller `EscalationPolicyController` lives at `org.fabt.notification.api.EscalationPolicyController`; URL prefix `/api/v1/admin/escalation-policy` honors the spec literally even though the project's other controllers don't use an `admin` segment — the auth gate (`COC_ADMIN+`) is what makes it admin and the prefix is a useful frontend grouping signal. **Spec deviation note:** the `admin` URL prefix is a deliberate exception to the project's "module-prefixed URL" convention; documented in the controller javadoc. Auth `COC_ADMIN+`. **Session 4 (2026-04-10).** (D7)
- [x] T-19: `PATCH /api/v1/admin/escalation-policy/{eventType}` — Bean Validation enforces structural shape including `@Size(min=1, max=50)` on the threshold list (Marcus Webb round-3 cap to prevent OOM via huge JSONB); service-layer `validateThresholds` enforces semantic rules (monotonic durations, valid roles, severity whitelist, unique threshold ids matching `[a-z0-9_]{1,32}`). Malformed ISO-8601 durations are caught at the controller boundary and surfaced as 400 instead of 500. Cache invalidated on PATCH. **Audit details** (Casey Drummond rounds 3+5): `tenantId`, `eventType`, `version`, plus `previousVersion` (when version > 1) so a subpoena can answer "what changed?" without joining audit_events back to escalation_policy. Versions are sequential within (tenant_id, event_type) — the atomic `MAX(version)+1` subquery in `EscalationPolicyRepository.insertNewVersion` makes `previousVersion = newVersion - 1` race-free. Publishes SSE `referral.policy-updated`. Auth `COC_ADMIN+`. **Session 4 (2026-04-10).** (D7, D8, D9)

### Backend — Audit Events

- [x] T-20: **SPEC DEVIATION (Session 3, 2026-04-10):** the spec asked for a new `AuditEventService.recordDvReferralAdminAction(...)` wrapper. Instead, `ReferralTokenService` uses the project's existing `ApplicationEventPublisher` → `AuditEventRecord` → `AuditEventService` listener pattern (the same one `UserService.publishAuditEvent` uses). Adding a second wrapper API would create two ways to publish audit events in the same module — inconsistent and confusing. The conformance is captured in the private `publishAudit(actorUserId, targetId, action, details)` helper at the bottom of `ReferralTokenService.java`. Zero PII enforcement is the caller's responsibility (the helper passes `details` through verbatim). (D8)
- [x] T-21 (Session 3 portion): Wired audit events for `DV_REFERRAL_CLAIMED` (claim), `DV_REFERRAL_RELEASED` (manual + timeout), `DV_REFERRAL_ADMIN_ACCEPTED` / `DV_REFERRAL_ACCEPTED` (role-aware), `DV_REFERRAL_ADMIN_REJECTED` / `DV_REFERRAL_REJECTED` (role-aware). Role differentiation goes through `userService.isAdminActor(...)` so the referral module never imports `auth.domain.User` directly (Alex Chen ArchUnit boundary). **Session 3 (2026-04-10).** Reassign + escalation policy update wiring is Session 4 (T-17, T-19). (D8)
  - *Persona — Casey Drummond:* "The actor's role distinguishes admin actions from coordinator actions in the audit trail. This is the chain-of-custody answer to a court subpoena." Verified end-to-end by `AuditEventCoverageTest.roleAwareAcceptAudit`.

### Backend — Tests

- [x] T-22: `EscalationPolicyServiceTest` — Mockito unit tests, 17 cases covering: id format + uniqueness, monotonic increasing duration, positive duration, severity whitelist, role whitelist, non-empty recipients, cache hit/miss, fallback to platform default, append-only versioning (`update` calls `insertNewVersion`), cache invalidation on PATCH, pre-population of by-id cache. **Session 3 (2026-04-10).** Lives at `backend/src/test/java/org/fabt/notification/service/EscalationPolicyServiceTest.java`.
- [x] T-23: `ReferralEscalationFrozenPolicyTest` — **load-bearing integration test (Casey Drummond + Riley Cho gate).** Creates referral A under platform default v1, publishes tenant policy v2 with a deliberately *different* shape (1.5h CRITICAL → COORDINATOR + 4h ACTION_REQUIRED → OUTREACH_WORKER), creates referral B, backdates A to 125min and B to 95min, runs the tasklet, asserts A fires `escalation.1h` + `escalation.2h` (v1) and B fires `escalation.1_5h` (v2), AND cross-asserts A does NOT fire v2's 1_5h, B does NOT fire v1's 2h. Also reads back the `escalation_policy_id` columns directly to prove the policies are different ids — the test cannot pass for the wrong reason. Two outreach workers used to side-step the "one PENDING per user per shelter" guard. **Session 3 (2026-04-10).**
  - *Persona — Casey Drummond + Riley Cho:* "If this test ever fails, the audit trail is compromised. Treat as critical."
- [x] T-24: `EscalatedQueueEndpointTest` — 4 integration tests: cross-tenant isolation (tenant A admin cannot see tenant B referrals), `expires_at ASC` ordering across three shelters with distinct expiries, zero-PII contract (no `callbackNumber`, no phone, no `specialNeeds` in the response body), and authorization gate (COORDINATOR → 403). Two distinct tenants set up via direct repository access since `TestAuthHelper` only manages one. **Session 3 (2026-04-10).**
- [x] T-25: `ClaimReleaseTest` — 5 integration tests: admin A claims/admin B blocked (409), `Override-Claim` header lets B steal, holder-only release (non-holder → 403), auto-release of an artificially expired claim, and **the load-bearing concurrency test**: two HTTP claims racing through a `CountDownLatch`, asserting exactly one OK + one 409 + the row holds the winner. The atomic `tryClaim` SQL closes the TOCTOU window. **Session 3 (2026-04-10).**
- [x] T-26: `ReassignTest` — **11 integration tests** covering: COORDINATOR_GROUP fan-out + payload PII assertion + audit `shelterId`/`actorRoles` enrichments (T-26a); COC_ADMIN_GROUP CRITICAL fan-out (T-26b); SPECIFIC_USER chain-broken state (T-26c); tasklet skips chain-broken (T-26d); SPECIFIC_USER missing targetUserId → 400 (T-26e); missing referral → 404 (T-26f); non-pending → 409 (T-26g); cross-tenant referral → 403 (T-26h); COORDINATOR_GROUP chain-resume after SPECIFIC_USER (T-26i); cross-tenant target user → 404 (T-26j); COC_ADMIN_GROUP chain-resume symmetry + `chainResumed:true` audit assertion (T-26k). **Session 4 (2026-04-10).**
- [x] T-27: role-aware accept/reject audit verification covered by `AuditEventCoverageTest.roleAwareAcceptAudit` and `AuditEventCoverageTest.rejectAuditAdminRole` (Session 3 portion); the "verify the referral is closed correctly after admin action" assertion is implicit because the existing accept/reject status-transition path is unchanged — only the audit type differs by actor. **Session 3 (2026-04-10).**
- [x] T-28: `EscalationPolicyEndpointTest` — **7 integration tests**: GET fallback to seeded platform default (T-28a); PATCH inserts new version + audit row + `shelterId`/`createdBy` shape (T-28b); PATCH non-monotonic durations → 400 (T-28c); PATCH invalid role → 400 (T-28d); PATCH malformed ISO duration → 400 (T-28e); COORDINATOR rejected on both verbs → 403 (T-28f); second-PATCH `previousVersion:1` audit assertion locks in Casey Drummond round-3 enrichment (T-28g). **Session 4 (2026-04-10).**
- [x] T-29: `AuditEventCoverageTest` — 5 integration tests covering CLAIMED, RELEASED (manual), RELEASED (timeout, NULL actor), role-aware accept (both branches), and admin reject. Reassign audit coverage is in `ReassignTest.coordinatorGroupReassignPagesShelterCoordinators` (T-26a). Policy-update audit coverage is in `EscalationPolicyEndpointTest.patchInsertsNewVersionAndReturnsIt` (T-28b) and `secondPatchRecordsPreviousVersion` (T-28g). **Session 3 + 4 (2026-04-10).**
- [x] T-30 (Session 3 portion): Re-ran the Session 1 baseline (`ReferralEscalationIntegrationTest`, `NotificationDiagnosticTest`, `NotificationPaginationTest`, `NotificationRlsIntegrationTest`, `SseNotificationIntegrationTest`, `SseStabilityTest`) plus all 5 new test classes plus ArchUnit (22 rules) — **92/92 green**. Tee log: `logs/coc-escalation-baseline-post-session3-fix.log`. The cache-bleed regression caught when `ReferralEscalationFrozenPolicyTest` polluted the in-memory `EscalationPolicyService` cache for sibling tests was fixed by exposing `clearCaches_testOnly()` and calling it from both classes' setUp/tearDown. **Session 3 (2026-04-10).** Final regression run will happen at end of Session 6 with full Playwright pass.

### Frontend — Types & Routing

- [x] T-31: Add `'dvEscalations'` to the `TabKey` union in `frontend/src/pages/admin/types.ts` — **shared types only** per the admin-panel-extraction convention. Tab-specific types (`EscalatedReferral`, `EscalationPolicy`, `EscalationPolicyThreshold`) live INSIDE `DvEscalationsTab.tsx`, NOT in `types.ts` (D20). The `EscalatedReferral` type mirrors the backend `EscalatedReferralDto` field-for-field including the round-5 `escalationChainBroken` boolean. (D20, archived `2026-04-07-admin-panel-extraction`)
- [x] T-32: Register the new tab in `frontend/src/pages/admin/AdminPanel.tsx` TABS array: `{key: 'dvEscalations', labelId: 'admin.dvEscalations'}`. Wire the route to lazy-load `DvEscalationsTab.tsx` via `React.lazy(() => import('./tabs/DvEscalationsTab'))` and add it to the `TAB_COMPONENTS` map. The W3C APG tabs pattern (Arrow/Home/End keyboard nav, roving tabindex, `role="tab"`/`role="tabpanel"`) is already in place — the new entry inherits it automatically. (D20, archived `2026-03-26-wcag-accessibility-audit` D7)

### Frontend — DvEscalationsTab

- [x] T-33: `DvEscalationsTab.tsx` — top-level component with two segmented sections ("Pending Queue" and "Escalation Policy"), default segment Queue. Mobile (<768px) hides the segment switch and shows Queue only — Policy editor is desktop-only. **Conformance:** import `color` from `theme/colors.ts`, `text/weight` from `theme/typography.ts`, `Spinner/ErrorBox/NoData/StatusBadge` from `pages/admin/components`, `tableStyle/thStyle/tdStyle/inputStyle/primaryBtnStyle` from `pages/admin/styles.ts`. Use `color.dv` family for DV accents (NOT `color.error`). NO hardcoded hex, NO numeric font sizes. `data-testid="dv-escalations-tab"`. (D2, D3, D20)
- [x] T-34: `EscalatedQueueTable.tsx` — desktop table component. Columns: Shelter, Time-to-expiry (live countdown), Population, Household, Urgency, Coordinator, Claim status, **Owner badge when `escalationChainBroken=true`** (Marcus Okafor war-room round 5 surface). Default sort ASC by time-to-expiry. Inline Claim button per row (44×44px minimum touch target). Row click opens `EscalatedReferralDetailModal`. Row background subtle red (`color.errorBg` token) when remainingMinutes < 15. Live SSE updates with row-highlight-then-fade. **Color independence (WCAG D4):** all status indicators use text + color, never color alone (chain-broken state shows "Owned by [name]" label, not just a dot; remaining-minutes shows the number, not just background color). `aria-live="polite"` region announces queue size changes. Every interactive element has `data-testid`. (D3, D20, archived `2026-03-26-wcag-accessibility-audit` D4)
- [x] T-35: `EscalatedQueueCardList.tsx` — mobile card list component (Linear progressive-disclosure pattern). One card per referral, visible fields: Shelter, time-to-expiry, single primary CTA (Claim). "More" button opens detail modal. Same chain-broken owner badge as T-34. Each card is keyboard-focusable (`role="article"` with `tabindex="0"`); the entire card is the click target with the Claim button as a sub-action that does NOT bubble. 44×44px minimum on every interactive element. (D3, D20)
- [x] T-36: `EscalatedReferralDetailModal.tsx` — modal opened from row click or "More" button. **Modal accessibility (WCAG D8 + UserEditDrawer existing pattern):** `role="dialog"`, `aria-labelledby` and `aria-describedby` set, focus moves into dialog on open, focus trap (Tab cycles within), Escape closes, focus returns to trigger on close, `data-testid="dv-escalation-detail-modal"`. Shows full referral context (zero PII — no callbackNumber, no clientName). Four action buttons: Claim/Release, Reassign, Approve placement, Deny. Approve/Deny use the danger-variant confirmation pattern with specific verb text ("Approve placement at [Shelter Name]", not "Confirm") and 5-min undo ribbon (UI state, not server state). (D3, D6, D20)
  - *Persona — Marcus Webb (added Session 1 round-table review):* the **Deny modal's reason text field SHALL display a prominent PII warning above it**, e.g. "Do not include client names, addresses, or other identifying information. This text is recorded in the audit trail." The audit event `DV_REFERRAL_ADMIN_REJECTED` stores the reason verbatim per `AuditEventTypes.java`; the admin is responsible for keeping it PII-free, but the UI must remind them. Tracked here so it doesn't get forgotten in Session 5.
- [x] T-37: `ReassignSubModal.tsx` — three target-type chooser using **disclosure pattern, NOT `role="menu"`** (D20, archived sse-notifications D10). Default selection: "To another coordinator" (searchable list of shelter-assigned coordinators). Second selection: "To CoC admin group". Third selection gated behind an "Advanced" `<details>` disclosure: "To specific user" with the warning that this breaks the escalation chain. The reason text field is required and SHALL display the PII warning per the persona note below. **Backend chain-resume contract (D14, war-room round 4):** if a previous SPECIFIC_USER reassign set `escalationChainBroken=true` and the admin now selects COORDINATOR_GROUP or COC_ADMIN_GROUP, the backend clears the flag and writes `chainResumed:true` in the audit row — the frontend doesn't need to do anything special, just display the resulting queue state from SSE. (D5, D20)
  - *Persona — Marcus Webb + Keisha Thompson (added Session 4 round-table review):* the **reason text field SHALL display a prominent PII warning above it**, identical to the Deny modal's warning (T-36). Backend stores `reason` verbatim in the `DV_REFERRAL_REASSIGNED` audit row but DELIBERATELY omits it from the broadcast notification payload (war room round 3 lock-in: `ReassignTest.coordinatorGroupReassignPagesShelterCoordinators` asserts the reason does NOT appear in `notification.payload`). The frontend modal is the only PII checkpoint for the reason field; the backend cannot enforce content. Tracked here so it doesn't get forgotten in Session 5.
- [x] T-38: `EscalationPolicyEditor.tsx` — desktop-only policy editor. Form fields per threshold: ISO duration input (e.g. "1h", "2h"), severity dropdown, recipients multi-select. **Client-side inline validation mirrors the server-side `EscalationPolicyService.validateThresholds`:** monotonically increasing durations, valid severities (`INFO|ACTION_REQUIRED|CRITICAL`), valid roles (`COORDINATOR|COC_ADMIN|OUTREACH_WORKER|PLATFORM_ADMIN`), unique threshold ids matching `^[a-z0-9_]{1,32}$`, max 50 thresholds (Marcus Webb war-room round 3 cap). Save button calls `PATCH /api/v1/admin/escalation-policy/dv-referral`; on `400` response, surface the structured error to the form. Mobile (<768px): renders a read-only message "Edit on a larger screen — this view is read-only on mobile" with form fields disabled and the Save button NOT in the DOM. (D7, D20)
- [x] T-39: `useDvEscalationQueue.ts` — React hook that **EXTENDS the existing `useNotifications` hook** rather than opening a parallel SSE stream (D20 — SSE connection budget). Adds 4 new `case` branches to the `onmessage` switch (`referral.claimed`, `referral.released`, `referral.queue-changed`, `referral.policy-updated`) that dispatch new `window` custom events (`fabt:dv-queue-update`, `fabt:dv-policy-update`). The hook itself is a thin wrapper: fetches initial queue via REST `GET /api/v1/dv-referrals/escalated`, listens for the custom events, exposes `{queue, loading, error, refresh}`. Uses the existing `@microsoft/fetch-event-source` connection — never reintroduces native `EventSource` or query-param token auth. (D9, D20, archived `2026-03-31-sse-stability`)

### Frontend — Banner CTA

- [x] T-40: Update `CriticalNotificationBanner.tsx` to add a primary CTA button: *"Review N pending escalations →"* (where N is the count of unread CRITICAL `escalation.*` notifications). Click navigates to `/admin#dvEscalations`. Banner is hidden when N = 0. i18n key: `notifications.criticalBanner.cta`. (D1)
  - *Persona — Keisha Thompson:* "Copy review before merge. The button text must center the work, not the urgency."
- [x] T-41: Update React Router (or admin panel anchor handling) to interpret `#dvEscalations` and pre-select the new tab.

### Frontend — i18n

- [x] T-42: Add ~30 new i18n keys to `frontend/src/i18n/en.json` and `es.json` (note: project uses flat locale files at `i18n/en.json`, NOT `i18n/locales/en.json`). Categories: tab label (`admin.dvEscalations`), queue column headers (`dvEscalations.queue.col.*`), action button labels (`dvEscalations.action.claim/release/reassign/approve/deny`), modal headers (`dvEscalations.modal.*`), validation messages (`dvEscalations.policy.error.*`), policy editor field labels (`dvEscalations.policy.field.*`), mobile read-only message (`dvEscalations.policy.mobileReadOnly`), chain-broken owner badge (`dvEscalations.queue.owner`), PII warning text for the reassign reason field (`dvEscalations.reassign.piiWarning`). **Keisha Thompson reviews all keys before merge** with the warmth-vs-urgency lens (no anxiety verbs; person-centered language). **Casey Drummond runs the legal language scan** (no overclaimed compliance language). **Spanish translation by a native speaker familiar with social-services terminology** — not a literal translation. (D20)
- [x] T-42a: **Hard build gate** — run `npm run build` (tsc strict + vite production build) and verify zero errors and zero warnings before declaring Session 5 complete. Per memory `feedback_build_before_commit` — frontend changes must build cleanly before commit. The build catches `tsc` strict-mode regressions (unused imports, implicit any, missing return types) that ESLint alone may miss.

### Frontend — Tests

- [x] T-43: Playwright `coc-escalation-banner.spec.ts` — fixture creates a pending DV referral, fast-forwards the escalation timer (or waits for the batch job), verifies the CRITICAL banner appears with the CTA button, clicks the CTA, asserts navigation to `/admin#dvEscalations` and the queue table shows the referral.
- [x] T-44: Playwright `coc-escalation-claim.spec.ts` — admin A claims a referral, admin B (in second browser context) sees the row update via SSE within 2s, admin B cannot claim without confirming the override, audit_events has both DV_REFERRAL_CLAIMED rows.
- [x] T-45: Playwright `coc-escalation-reassign.spec.ts` — admin reassigns to coordinator group via the default tab, asserts the SSE `referral.queue-changed` event fires, the row status updates, audit event recorded.
- [x] T-46: Playwright `coc-escalation-policy.spec.ts` — admin opens the policy editor, changes T+2h to T+3h, saves, verifies the SSE `referral.policy-updated` event fires, verifies a NEW referral fires CRITICAL at T+3h (not T+2h), verifies an EXISTING referral still fires per the OLD policy (frozen-at-creation).
- [x] T-47: Playwright `coc-escalation-mobile.spec.ts` — mobile viewport (375x667), verifies the queue shows as a card list, the policy editor section shows the read-only message, claim works from a card.

### Documentation

- [ ] T-48: Create `docs/architecture.md` — Mermaid system overview replacing `docs/architecture.drawio` (which is preserved as a v0.21–v0.28 historical reference). Sections: System Overview (Cloudflare → nginx → Spring Boot → PostgreSQL), Module Map (modular monolith boundaries), DV Escalation Data Flow (referral_token → escalation_policy frozen FK → batch job → notification → SSE → admin queue). All diagrams in Mermaid for natural GitHub rendering. (D10)
  - *Persona — Alex Chen:* "Predates v0.31.0. The Mermaid replacement must accurately reflect the current modular monolith including persistent notifications, RLS simplification, and the new escalation policy module."
- [ ] T-49: Create `docs/business-processes/dv-referral-end-to-end.md` — **companion parent doc** establishing the full DV referral flow that the escalation doc branches from. Sections: actor inventory (outreach worker, coordinator, CoC admin, survivor) with persona links, Mermaid sequenceDiagram of the happy path from search → request → screen → accept → warm handoff → closure, role × phase matrix, Mermaid stateDiagram-v2 of the `referral_token` state machine (PENDING/ACCEPTED/REJECTED/EXPIRED — the base machine that escalation extends with CLAIMED), persona traceability. This is the parent reference that `dv-referral-escalation.md` (T-50) builds on. (D10)
- [ ] T-50: Create `docs/business-processes/dv-referral-escalation.md` — escalation branch of the parent flow (T-49). Sections: when escalation fires (the role × time matrix), Mermaid sequenceDiagram of the escalation timeline (T+1h reminder → T+2h CRITICAL → T+3.5h all-hands → T+4h expired) including the new admin claim/release/reassign/act actions, edge case section (coordinator offline, two admins claim simultaneously, survivor cancels mid-flow, expiry race conditions, mid-day policy change with frozen-at-creation answer), persona traceability. Cross-references the parent doc instead of inlining context. **Both T-49 and T-50 reviewed together by Devon Kessler (training), Marcus Okafor (CoC admin), and Keisha Thompson (dignity) before merge as a single ~60-minute joint walkthrough — schedule one session covering both files, do not request reviews independently.** (D10)
- [ ] T-51: Update `docs/FOR-DEVELOPERS.md` — new section "Escalation Policy and Frozen-at-Creation" documenting the data model, the policy versioning rule, the new endpoints, and the pattern for adding future event types beyond `dv-referral`.

### Performance / Operational

- [ ] T-52: **Escalation batch duration baseline (reframed 2026-04-12 per Alex Chen review).** The original wording assumed an existing Micrometer timer around the batch job — Session 7 discovery: no such timer exists. `ReferralEscalationJobConfig` has no explicit timer and `BatchJobScheduler` has no MeterRegistry injection. Spring Batch 5 auto-emits `spring.batch.step{name=checkEscalationThresholds}` but that's not a `fabt.*` metric and the project convention is unified fabt-prefixed operational metrics (see `ObservabilityMetrics.bedHoldReconciliationDurationTimer` as precedent). **Reframed task:** (a) add `fabt.escalation.batch.duration` as a new histogram timer wrapping the tasklet body — this is the SLO baseline for future regressions; (b) acknowledge in IMPLEMENTATION-NOTES that pre-refactor p95 cannot be recovered post-hoc, so "unchanged vs baseline" verification is literally impossible for this refactor; (c) add `ReferralEscalationPerfTest` integration test with a 1000-referral fixture asserting tasklet wall clock under a generous budget — this is the regression guard going forward. Spring Batch's auto-emitted step timer is retained as the secondary signal alongside the new fabt timer. (Alex Chen / Sam Okafor, 2026-04-12)
- [ ] T-53: **New Micrometer metrics** — four total, extending `ObservabilityMetrics` following the established `fabt.*` factory pattern:
  - `fabt.escalation.batch.duration` (Timer, histogram) — covered under T-52
  - `fabt.escalation.policy.*` cache metrics via `CaffeineCacheMetrics.monitor(...)` — emits the `cache.gets{cache=~"fabt.escalation.policy.*",result="hit|miss"}` family plus `cache.puts`, `cache.evictions`. **Hit rate is a Grafana/PromQL formula**, not a single emitted metric — reframed from the original wording `fabt.escalation.policy.cache.hit-rate` (Alex Chen review: don't bend Micrometer to match a guessed metric name). Requires `.recordStats()` on both Caffeine builders in `EscalationPolicyService` — without it, `CaffeineCacheMetrics` silently emits zeros. Add a cache-metrics integration test that exercises both caches and asserts non-zero `cache.gets{result="hit"}` on the registry to prevent silent regression.
  - `fabt.dv-referral.claim.duration` (Timer, histogram) — tagged `outcome=success|conflict|error`. Wraps `ReferralTokenService.claimToken`. Low-cardinality tags only — never tag by tenantId, shelterId, userId, or referralId (Alex Chen cardinality guardrail).
  - `fabt.dv-referral.claim.auto-release.count` (Counter, no tags) — incremented by `released.size()` inside `ReferralTokenService.autoReleaseClaims` after `repository.clearExpiredClaims()` returns. Grafana panel should show `rate(...[5m])` for the auto-release frequency.
  - **Grafana panels:** 4 panels on the CoC escalation dashboard (create new `observability/grafana/dashboards/coc-escalation.json` if no dashboard exists yet): escalation batch p95 duration, cache hit rate (PromQL formula from `cache.gets`), claim duration p95 histogram, auto-release count rate.

### Pre-Merge

- [ ] T-54: Run full backend test suite (`mvn clean test`). All tests green, no regressions, ArchUnit boundaries intact (escalation_policy stays in `notification` module). Tee to `logs/backend-full-coc-escalation.log`.
- [ ] T-55: Run full Playwright suite through nginx. All tests green. Tee to `logs/playwright-coc-escalation.log`.
- [ ] T-56: Open PR against main, link to issue #82 and this OpenSpec change. Hold for scans (memory: `feedback_release_after_scans`).
- [ ] T-57: After merge: bump `pom.xml` version to **v0.35.0** (retargeted from the original v0.33.0 plan after v0.34.0 bed-hold-integrity shipped first), promote CHANGELOG `[Unreleased]` → `[v0.35.0]`, update `e2e/playwright/deploy/post-deploy-smoke.spec.ts` test 1 assertion from `'v0.34'` to `'v0.35'`, create `docs/oracle-update-notes-v0.35.0.md` (mirror the v0.34.0 template), tag, GitHub release. Comment on issue #82 with the release link. Close #82. *(The v0.32.2 webhook fix referenced in the original plan shipped in v0.32.2/v0.34.0 already — it is no longer bundled with this release.)*
- [ ] T-55a: **API-level Playwright coverage for `/manual-hold`** (subsumes Task #3 from the v0.34.0 work queue, rolled into this effort per founder direction 2026-04-11). New spec `e2e/playwright/tests/manual-hold.spec.ts` with 4 scenarios per Riley Cho's Option B (API calls via `page.request.post`, not UI flow, because there's no frontend manual-hold UI yet): (a) coordinator creates manual hold on assigned DV shelter — expect HTTP 201 and reservation appears in reservations panel with "Manual offline hold:" note prefix; (b) coordinator on unassigned shelter — expect HTTP 403 with `fabt_http_access_denied_count_total` Micrometer counter increment proving controller-level rejection; (c) admin (cocAdmin) bypasses assignment check — expect HTTP 201; (d) reservation appears in reservations panel with proper expires_at countdown. Use `data-testid` locators for the post-assert UI check (not DOM selectors). Header comment: "API-only for now; upgrade to user-flow when the Create Manual Hold UI ships in a future change."
- [ ] T-V50: **V50 — cocadmin dv_access flip (Oracle demo seed fix).** Create `backend/src/main/resources/db/migration/V50__fix_cocadmin_dv_access.sql`. Surgical UPDATE targeting hardcoded cocadmin UUID `b0000000-0000-0000-0000-000000000003`, guarded by `WHERE dv_access = false` for idempotency. Required on the Oracle demo VM where cocadmin was originally seeded with `dv_access=false` and seed-data.sql only runs on initial Postgres init. Real-tenant CoC admins must be granted DV access via the admin UI per-user (Casey Drummond war-room round 5 guardrail — other tenants may intentionally have DV-free jurisdictions). Originally drafted as V44 in the pre-v0.34.0 deploy plan memory; renumbered to V50 after bed-hold-integrity shipped V44/V45 and the four coc-admin-escalation migrations shifted to V46-V49. **Post-rebase commit 3 of 3 in the Session 7 rebase cleanup.**
- [ ] T-58: After release: `/opsx:verify coc-admin-escalation` → `/opsx:sync coc-admin-escalation` → `/opsx:archive coc-admin-escalation`.
