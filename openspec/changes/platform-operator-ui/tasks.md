# Tasks: platform-operator-ui

## 1. Setup

- [x] 1.1 Pull main + branch off `feature/platform-operator-ui` from current HEAD (in code repo `finding-a-bed-tonight/`) — branched at `9dc67ea` (v0.53.0 tag), already up-to-date with origin
- [x] 1.2 Confirmed v0.53 backend baseline healthy via `mvn compile -DskipTests` (clean build, no errors). Full `mvn test` runs at 2.6 post-implementation; if any pre-existing test fails there, we know we broke baseline.

## 2. Backend additions (narrow un-freeze)

- [x] 2.1 Created `PlatformOperatorMeDto` record. Implementation revealed need for V90 Flyway migration `platform_user_get_me()` SECURITY DEFINER function (existing `platform_user_lookup_by_id` returns only credential-shaped fields). V90 collision with reentry-spec resolved by renumbering reentry V90-V93 → V91-V94 (33 references across 4 files).
- [x] 2.2 Added `GET /me` — returns DTO with id/email/mfaEnabled/lastLoginAt/mfaEnrolledAt/backupCodesRemaining. Wrong-scope tokens get 403 via new `PlatformScopeMismatchException` (not 401, per Marcus's defense-in-depth split: "auth fine but not for here"). Tenant JWT rejected at validateToken → 401 (existing pattern).
- [x] 2.3 Added `POST /logout` — server-side no-op (returns 204). Verified by IT that `last_login_at` does NOT mutate.
- [x] 2.4 `PlatformAuthControllerMeTest` (4 tests): all-fields happy path + secrets-not-leaked assertion, missing-token → 401, mfa-setup-token → 403, garbage-token → 401. **GREEN**.
- [x] 2.5 `PlatformAuthControllerLogoutTest` (3 tests): 204 + no DB mutation, missing-token → 401, mfa-setup-token → 403. **GREEN**.
- [x] 2.6 Full backend `mvn test` → **1285/1285 GREEN** including the 7 new platform-operator-ui IT tests + MigrationLintTest (V90 added to SECURITY_DEFINER_ALLOWLIST with citation following V87/V88 precedent)

## 3. Frontend foundation

- [x] 3.1 `--color-platform` token added to `frontend/src/global.css` (light + dark sections, both burnt orange #C2410C with 5.4:1 white-text contrast comment). `colors.ts` exports `color.platform`. axe-core sweep is task 6.9 (deferred to test phase).
- [x] 3.2 `VITE_PLATFORM_UI_ENABLED` typed in new `frontend/src/vite-env.d.ts` (augments ImportMetaEnv); documented in `frontend/.env.example`. Vite reads from `.env*` automatically — no vite.config.ts change needed.
- [x] 3.3 `frontend/src/auth/PlatformAuthContext.tsx` created. Sibling to AuthContext (NOT generalized). Exposes `usePlatformAuth()` returning `{jwt, claims, operatorId, isAuthenticated, isMfaVerified, login, logout}`. Cross-tab `storage` event listener triggers logout in other tabs when sessionStorage clears.
- [x] 3.4 `frontend/src/pages/platform/helpers/platformJwt.ts` created. `PLATFORM_JWT_STORAGE_KEY = 'fabt.platform.jwt.v1'`. `parseClaims()` is signature-unverified (server is the authority). `isExpired()` synchronous + cheap. `secondsUntilExpiry()` for the banner countdown (4.10).
- [x] 3.5 `frontend/src/pages/platform/helpers/platformApi.ts` created. Module-level `isHandling401` flag prevents concurrent 401s from double-navigating. 403 from `/me` redirects to `/platform/mfa-enroll` without wiping sessionStorage. Test-only `__resetIsHandling401ForTests()` exposed.
- [x] 3.6 `frontend/src/pages/platform/PlatformProtectedRoute.tsx` created. Synchronous checks: JWT present + `iss === 'fabt-platform'` + `isExpired` + scope-based routing (mfa-setup → /mfa-enroll, mfa-verify → /mfa-verify, else require `mfaVerified=true`). `allowMfaSetupScope` prop for the enroll route.
- [x] 3.7 `App.tsx` wires `/platform/*` route. Top-level `if (import.meta.env.VITE_PLATFORM_UI_ENABLED === 'true')` guards the `lazy()` import literal — Rollup dead-code-eliminates when false. CI assertion at `frontend/scripts/verify-platform-chunk-tree-shake.sh` runs in `ci.yml` frontend job. **J1 (round 6):** broadened from `find PlatformPlaceholder-*.js` to `find -iname Platform*.js` + a `≥5` chunk floor on the flag=true build; would otherwise have silently passed regressions that emitted Login/MfaEnroll/MfaVerify chunks despite flag=false. Currently emits 6 platform chunks on flag=true (Layout, Login, MfaEnroll, MfaVerify, Placeholder + platformApi shared chunk). `PlatformAuthProvider` wraps the BrowserRouter inside `AuthProvider`.
- [x] 3.8 Tenant `/login` page unchanged — confirmed via grep on App.tsx (LoginPage import only) and no `/platform` reference in `LoginPage.tsx`.
- [x] 3.9 Section 3 ships a `PlatformPlaceholder` stub; the real cross-link to tenant `/login` is part of the `PlatformLogin` page in task 4.1. This task moves to verification at the §6 testing checkpoint.

## 4. SPA routes

- [x] 4.1 Built `frontend/src/pages/platform/PlatformLogin.tsx`. Heading "Platform Operator Sign-In", subheading with `data-testid="platform-login-tenant-crosslink"` to `/login`. POSTs to `/api/v1/auth/platform/login`. Branches on `scope` field: `mfa-setup` → `/platform/mfa-enroll`, `mfa-verify` → `/platform/mfa-verify`. Shows "Session expired" toast (one-shot from `fabt.platform.toast.session-expired` sessionStorage key set by banner countdown). 401/429/network all show generic / specific error without leaking email-vs-password. **J5:** logs network errors via `console.error` so CORS / DNS / firewall issues can be diagnosed in browser DevTools (vs silently appearing as "couldn't reach server").
- [x] 4.2 Built `frontend/src/pages/platform/PlatformMfaEnroll.tsx`. Three phases: `loading` (initial /mfa-setup fetch), `scan` (QR canvas + manual secret + supported-authenticators list), `codes` (BackupCodesDisplay). On mount POSTs `/auth/platform/mfa-setup` (idempotent backend → reload-mid-enrollment recovers same secret). QR rendered via `qrcode` library to a `<canvas>` with `role="img"` + `aria-label` describing QR + `aria-describedby` pointing to manual-secret element id. Confirm POSTs `/auth/platform/mfa-confirm`; on success validates `body.token` non-empty (J3), stores access token via `login()`, transitions to codes phase. If MFA-setup token expired, platformFetch's 401 handler kicks back to /platform/login.
- [x] 4.3 Built `frontend/src/pages/platform/components/BackupCodesDisplay.tsx`. Codes rendered as text-node `{code}` interpolation in an `<ol>` (no `dangerouslySetInnerHTML` — `eslint-disable react/no-danger` comment with regression-guard rationale at file top). Confirmation checkbox + Continue (disabled until checked). Print + Copy-to-Clipboard each gated by `ConfirmActionModal`. Print: hides screen view (`display: none`) while `PrintFriendlyCodes` mounts and `window.print()` fires after a 50ms paint defer. **Round 6 M4:** uses `afterprint` event listener (Safari-safe) instead of inline restore; Chrome/Firefox/Safari all wait for the spool then fire afterprint. Copy: tracks the auto-clear timer ID in a `useRef` so re-copy cancels the prior timer (M2/S1: prevents the 30s clipboard-trash bomb where T1 wipes whatever the operator pasted next). Surfaces clipboard-write failure as inline `role="alert"` error (M3). Cleanup useEffect cancels timer on unmount. **Cache-Control/Pragma backend headers verification:** deferred to §6 Playwright (network-trace assertion). Note: H5 unit-test for `<script>`-as-literal-text deferred to §6 Playwright; component-level rendering tests not feasible without RTL dep.
- [x] 4.4 Built `frontend/src/pages/platform/components/PrintFriendlyCodes.tsx`. Component injects a `<style>` element with `@media print` CSS into `document.head` on mount, removes on unmount (CSP-compatible: stylesheet text is static, not derived from user input). `@media print` rule sets `body * { visibility: hidden; }` THEN un-hides only the `.platform-print-only` subtree, which renders the bare heading "Platform Operator Backup Codes" + the 10 codes in a monospace `<ol>` + the notice "Store securely. These codes can authenticate as your account." No operator email, URL, timestamp, or QR — Marcus's stripped-print-view condition.
- [x] 4.5 Built `frontend/src/pages/platform/components/ConfirmActionModal.tsx` — three variants (`print`, `copy`, `destructive`) with Cancel default-focused, locked button labels per spec, typed-confirmation field on destructive variant. Render-prop callback shape (`onCancel`/`onConfirm`); slug-typed-match logic local to component.
- ~~Original task description (now done):~~ Build `frontend/src/pages/platform/components/ConfirmActionModal.tsx` — generic modal with three variants:
  - **Print variant**: copy *"These codes will be sent to your printer or saved as a PDF. They will appear in your OS print queue and may be retained by network printers. Continue?"*; primary button labeled exactly "Cancel" (default-focused); secondary button labeled exactly "Print Anyway"
  - **Copy variant**: copy *"These codes will be placed on your system clipboard. Clipboard managers and pasted-into apps may retain them. The clipboard will auto-clear in 30 seconds. Continue?"*; primary button labeled exactly "Cancel" (default-focused); secondary button labeled exactly "Copy Anyway"
  - **Destructive-action variant**: typed-confirmation field requiring exact match of tenant slug; primary button "Cancel" (default-focused); secondary button matches action verb (e.g. "Suspend Tenant")
- [x] 4.6 Built `frontend/src/pages/platform/PlatformMfaVerify.tsx`. Mode toggle between `totp` (6-digit numeric, `inputMode="numeric"`, `autoComplete="one-time-code"`) and `backup-code` (12-char text). POSTs `/auth/platform/login/mfa-verify`; on success `login(token)` + navigate `/platform/dashboard`. Error branches: `error: account_locked` → "Too many failed attempts. Account locked for 15 minutes…" + disable input; `attemptsRemaining: N` → "Code invalid. N attempts remaining…"; `error: invalid_platform_token` (warroom round 6 A6) → set session-expired toast + redirect to `/platform/login`; otherwise generic "Code invalid"; network catch → "Couldn't reach server…". J3: validates `body.token` non-empty before `login()`. **Backend extension (round 6 A1):** V90 added `platform_user_record_failure_with_state` SECURITY DEFINER function returning TABLE(now_locked, account_locked, attempts_used). PlatformAuthService.verifyMfaWithState() returns the richer record. PlatformAuthController emits `{error, attemptsRemaining}` for "wrong code" and `{error: account_locked}` for lockout. Wire contract verified by `PlatformAuthMfaVerifyResponseTest` (4 tests).
- ~~Original task description (now done):~~ Build `frontend/src/pages/platform/PlatformMfaVerify.tsx` — TOTP input (6 digits) OR backup code input (8 chars); posts to `/auth/platform/login/mfa-verify`; on success stores JWT in sessionStorage + redirects to `/platform/dashboard`. Render 3 error states per spec:
  - **Wrong TOTP/backup code (401 with attempts-remaining):** display "Code invalid. X attempts remaining before lockout."; clear input + refocus; submit re-enables for retry
  - **Lockout reached (401 lockout response):** display "Too many failed attempts. Account locked for 15 minutes. If you've lost your phone, use a backup code."; disable TOTP input; keep "Use backup code instead" link active
  - **Network error (no HTTP response):** display "Couldn't reach server. Check your connection and try again."; submit re-enables for retry; sessionStorage NOT wiped (MFA-verify scoped token remains valid)
- [x] 4.7 Built `frontend/src/pages/platform/PlatformDashboard.tsx`. h1 heading, operator metadata header (email/last-login/mfa-enrolled/backup-codes badge with WCAG 1.4.1 text urgency label), category sections (h2) with action cards (h3), placeholder card for empty Operator Management category. Consumes new `usePlatformMetadata()` context to dedupe `/me` fetch with banner. **Round 7/8 hardening:** removed broken destructive-modal wiring (`expectedSlug: ''` typed-slug bypass + no-op confirm stub); added defensive throw in `handleActivate` for destructive/flag-gated actions to prevent Slice E timebomb.
- [x] 4.8 Built `frontend/src/pages/platform/platformActions.ts`. 7 actions across 3 categories (5 lifecycle, 0 operator, 2 system). Includes `buttonLabel` field per action (round 7 — replaces incoherent concat like "Open hard-delete tenant"). `tenant-list` flag-gated (round 7 — `/api/v1/tenants` requires PLATFORM_OPERATOR auth which `window.open` can't supply).
- [x] 4.9 Built `frontend/src/pages/platform/components/PlatformActionCard.tsx`. Disabled-with-tooltip when flag-gate is off; copy "This action is disabled in this deployment. Contact platform engineering to enable."
- [x] 4.10 Built `frontend/src/pages/platform/components/PlatformOperatorBanner.tsx` + `PlatformLayout.tsx` (Outlet-style layout wrapper). Banner shows masked operator email (`maskEmail` helper, 9 unit tests), 1s-tick countdown ("Session expires in X:XX"), amber at <=2min, red at <=30s, redirects to `/platform/login` with toast key on tick-zero (M1: gated by `redirectFiredRef` to prevent re-firing). Logout button POSTs `/api/v1/auth/platform/logout` (best-effort) then clears sessionStorage + redirects. `useOperatorMetadata` hook fetches `/me` on mount, distinguishes 410 anonymized as separate `anonymized` flag (J2). Banner force-logout on anonymized=true. Banner suppressed on routes where no JWT exists (login screen). aria-live switches to "off" when countdown >2min to suppress per-second screen-reader announcements (S5). PlatformLayout has its own `<Suspense>` around `<Outlet />` so cold-loading nested chunks doesn't blank the screen (A2). Component-level testing deferred to §6 Playwright (no React Testing Library available without dep research).

## 5. Telemetry + observability

- [x] 5.1 Audited existing counters: `fabt.platform.login.failures{reason}` + `fabt.platform.user.locked_out` + `fabt.platform.admin.action{action,outcome}` already shipped in G-4.5. Missing piece for the §5.4 alerts was MFA-verify outcome; **added** `fabt.platform.mfa.verify{outcome=success|failure}` to PlatformAuthService — wired in success branches (TOTP + backup-code) AND `failureWithState`. Counter is documented in `docs/observability/platform-admin-monitoring.md` under "v0.54 (F11 §5.1)".
- [x] 5.2 Followed G-4.5 convention (panel definitions in markdown, defer JSON commit until Grafana provisioning lands). Added Panel 6a (MFA-verify outcome rate) + Panel 6b (platform-operator backend HTTP status) to `docs/observability/platform-admin-monitoring.md`. Renumbered prior Panel 6 → Panel 7.
- [x] 5.3 Comment added to `frontend/src/pages/platform/components/BackupCodesDisplay.tsx` JSDoc reaffirming Marcus condition #3 (no analytics, no Sentry, no console.log of code material) + warning future contributors to audit any third-party SDK for implicit DOM scraping.
- [x] 5.4 Authored `deploy/prometheus/f11-platform-operator-ui.rules.yml` with two new alerts: `FabtPlatformMfaFailureSpike` (warning, rate > 1/min sustained 5 min) + `FabtPlatformBackend5xx` (critical, any 5xx on `/api/v1/auth/platform/*` over 2 min). Spec's third alert `PlatformLockoutTriggered` (critical/page) consolidated against existing G-4.5 `FabtPlatformUserLockedOut` (info, single lockout = not page-grade per the G-4.5 design rationale that auto-clear via cron + manual unlock makes a single event recoverable without escalation). Trade-off documented in the new rules file header + `platform-admin-monitoring.md` alerts table.

## 6. Testing

- [x] 6.1 Coverage met by composition: happy-path login is exercised in `platform-ui-routing.spec.ts` (line 48-101 — successful first-login + subsequent-login redirect targets); MFA-verify happy path inferred from `platform-ui-mfa-verify-errors.spec.ts` (the spec mocks success in fixtures); dashboard render of all expected fields is `platform-ui-dashboard.spec.ts` (8 tests — 7 cards × disabled state, backup-codes urgency text, button labels, popup behavior). All run isolated against mocked backend — no shared-seed flake.
- [x] 6.2 Authored `e2e/playwright/tests/platform-mfa-enroll.spec.ts` — 5 tests: scan-phase QR + manual secret + supported authenticators visible, codes phase mounts after submit with all 10 codes asserted by testid (`platform-backup-code-${i}` × 10), Continue gated by acknowledgement checkbox, Continue lands on /platform/dashboard with banner mounted (verifies the round-12 deferred-login fix end-to-end), second-login `mfa-verify` scope routes to `/platform/mfa-verify` not `/platform/mfa-enroll`. All mocked-backend, isolated.
- [x] 6.3 Authored `e2e/playwright/tests/platform-print-codes.spec.ts` — 5 tests: print modal opens but does NOT auto-spool window.print() (Marcus condition #1 — verified via window.print spy, count == 0 until confirm clicked), Cancel on modal preserves count == 0, confirm-action fires window.print exactly once after the 50ms paint defer, copy modal blocks navigator.clipboard.writeText similarly, back-button after Continue does NOT resurrect backup-codes-list. **Caught a real production gap during authoring**: the controller's `/mfa-setup` response did NOT carry `Cache-Control: no-store` despite the user-guide + JSDoc claiming it did. Fixed `PlatformAuthController.mfaSetup()` to set `Cache-Control: no-store, no-cache, must-revalidate, private` + `Pragma: no-cache` + `Expires: 0`. Added a 9th test to `PlatformAuthControllerMeTest` asserting the success-path header (Playwright can't reach success without the JWT signing secret, so the header assertion lives at the backend IT layer per the spec rationale). Backend mvn now 9/9 GREEN on that test class (was 8/8).
- [x] 6.4 Coverage met by composition: countdown rendering at `platform-ui-banner.spec.ts:38-71` (asserts "Session expires in 1[45]:" pattern); session-expired toast at `platform-ui-banner.spec.ts:156-173` (tests one-shot toast key consumption on `/platform/login`); expired-JWT-on-direct-nav at `platform-ui-routing.spec.ts:204-219` (synchronous guard kicks the operator back to /login). The tick-zero auto-redirect (banner countdown reaching 0:00 in real time) is intentionally NOT exercised in CI — it requires either a 15-min wait or page.clock manipulation that hasn't been wired; the `redirectFiredRef` gate + `secondsUntilExpiry` math are unit-test-pure and proven via `platformJwt.test.ts:80-94`.
- [x] 6.5 Covered by `platform-ui-dashboard.spec.ts:86-105` (5 lifecycle cards disabled with the "This action is disabled in this deployment…" tooltip, asserted by `toBeDisabled` + `toHaveAttribute('title', /disabled in this deployment/i)`) AND `:107-134` (force-clicking a disabled button does NOT open a new tab — proves the click never fires `window.open`/POST). Round-8 N1 added a defensive throw in `handleActivate` for destructive/flag-gated actions (`PlatformDashboard.tsx:80-119`) so a flag-flip-without-Slice-E can't silently misfire either.
- [x] 6.6 Fixture exists at `e2e/playwright/helpers/auth/platform-operator.ts` (kebab-case, not the camelCase the spec listed) — used by `platform-totp-lockout.spec.ts` and the smoke spec. The new round-7-9 platform-ui specs deliberately use mocked backend (page.route) rather than the fixture, for shared-seed isolation per `feedback_isolated_test_data.md`.
- [x] 6.7 Wave 1 ran full `mvn test` against the v0.54 branch: **1294/1294 GREEN** (was 1285 baseline + 9 new across V90-bound IT tests). Log at `logs/wave1-mvn-test.log`.
- [x] 6.8 Wave 1 ran full Playwright suite via `BASE_URL=http://localhost:8081 npx playwright test --trace=on`: **410 passed, 10 failed, 3 skipped (36.4 min)**. The 10 failures are pre-existing test-isolation flake in DV-outreach (3), DV-referral (2), observability (2), and persistent-notifications (3) — all in code paths untouched by this branch and matching the `feedback_isolated_test_data.md` 409-conflict / drifting-count anti-pattern. Logs at `e2e/playwright/logs/wave1-full-playwright.log` + warroom round-10 verified the failure files were not modified by this branch.
- [x] 6.9 Authored `e2e/playwright/tests/platform-ui-a11y-csp.spec.ts` — 6 axe-core scans (3 routes × {light, dark}) using `page.emulateMedia({ colorScheme })` + `AxeBuilder.withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'])`. **Caught 1 real WCAG fail** in light theme: `opacity: 0.6` on disabled action cards effectively reduced text-secondary contrast to 3.25:1 (axe correctly reports rendered color, not source token). Fixed `PlatformActionCard.tsx` by removing the opacity — disabled state now communicated solely by the disabled button + tooltip + cursor:not-allowed (text stays at full token contrast: 10.7:1 light / 10.3:1 dark). Same opacity→token anti-pattern resolved earlier per `project_accessibility_contrast_failures.md`. Final: **6/6 a11y GREEN**, 26/26 existing platform-ui specs unaffected. /platform/mfa-enroll skipped per task scope (manual cross-authenticator QA per §6.10 is canonical for that page).
- [ ] 6.10 **DEFERRED to user (hands-on phones).** Authored `docs/operations/platform-operator-mfa-compatibility.md` with the 5×2 fillable matrix + methodology + known-issues section. Operator runs the QA against `./dev-start.sh --nginx` (or post-deploy with their own auth) and commits the matrix as a fast-follow. Spec accepts the matrix landing AFTER the v0.54 tag — the deploy is not blocked on this row.
- [x] 6.11 Authored `e2e/playwright/tests/platform-training-walkthrough.spec.ts` — single narrated end-to-end test with 11 console-logged "[training §N]" steps. Covers login → mfa-enroll → confirm → backup codes (all 10 asserted) → ack checkbox → Continue → dashboard banner + countdown + masked email + urgency badge → disabled lifecycle card with tooltip → enabled system-health card → logout → sessionStorage cleared. **Skipped in CI** via `test.skip(!!process.env.CI, ...)` per spec ("NOT part of CI gates; manually invoked"). Operators run via `npx playwright test platform-training-walkthrough.spec.ts --headed` to watch the rehearsal in a browser. User-guide section 1 reference will land in v0.55 — the spec itself is committed.
- [x] 6.12 CSP assertions in `platform-ui-a11y-csp.spec.ts`: 3 routes (login/dashboard/mfa-verify) × required directives present (`default-src 'self'`, `script-src 'self'`, `frame-ancestors 'none'`, `base-uri 'self'`, `form-action 'self'`) AND forbidden tokens absent (`'unsafe-eval'`, no `'unsafe-inline'` in script-src specifically). Plus a 4th test that intercepts browser console errors during full dashboard load and asserts ZERO `Content Security Policy` / `Refused to` violations — this catches a future dep change that adds eval/inline (e.g. a new chart lib). nginx CSP at `infra/docker/nginx.conf:12,107` is the source of truth. Final: **4/4 CSP GREEN**.

## 7. Documentation

- [x] 7.1 Authored `docs/operations/platform-operator-user-guide.md` — 2251 words across all 8 mandatory sections. Section 4 (destructive actions) carries an explicit v0.54-status caveat noting the typed-confirm flow is forward-compatible documentation for Slice E (cards ship flag-gated disabled in v0.54 per design D3). Section 6 includes the F45 follow-up reference for the v0.55 sole-operator reset tool. Internal links relative.
- [x] 7.2 Authored `e2e/playwright/tests/capture-platform-operator-screenshots.spec.ts` — 6 captures pinned at 1440×900 + light theme via `test.use({ viewport, colorScheme })`. **Caught a real production bug** during capture authoring: the `setPhase('codes')` flow called `login(token)` synchronously, swapping the JWT to post-MFA-access; the `/mfa-enroll` route guard (`requiredScope='mfa-setup'`) re-evaluated and navigated the operator away BEFORE backup codes rendered — so production operators would never see their codes. Fixed `PlatformMfaEnroll.tsx` to defer `login(token)` into the BackupCodesDisplay onContinue handler (token held in `pendingAccessToken` state). 6 screenshots committed to `docs/operations/screenshots/platform-operator/`.
- [x] 7.3 Added user-guide link in BOTH `/platform/mfa-enroll` scan phase ("First time enrolling? See the Platform Operator User Guide") AND `/platform/dashboard` header ("First time? See the Platform Operator User Guide"). URL pinned in new `frontend/src/pages/platform/constants.ts` so future doc moves are a one-line edit. Created `docs/operations/README.md` indexing the user guide + screenshots dir + cross-references to runbook + observability docs. Anchor testids: `platform-mfa-enroll-user-guide-link`, `platform-dashboard-user-guide-link`.
- [x] 7.4 `CHANGELOG.md` `[Unreleased]` block authored — covers F11 SPA + GET/me + POST/logout + V90 migration + new mfa.verify counter + 2 new alert rules + observability doc updates + verification numbers (1294/1294 backend + 195/195 vitest + 36 platform-ui Playwright + 12 a11y+CSP) + the two real WCAG fixes the axe sweep caught.
- [x] 7.5 Authored `docs/oracle-update-notes-v0.54.0.md` per the v1 runbook template. All 8 mandatory sections present + a "What's new" preface. Two-stage deploy (Stage A flag-off cold deploy → Stage B flag-on activation) with concrete rollback procedure (~6min RTO; backend/DB unchanged between stages). Section 5 step 11 documents the SSH-tunnel-to-:8081 access pattern WITHOUT committing the tunnel command or demoguard bypass posture, per `feedback_platform_login_via_ssh_tunnel.md`. Post-deploy gates verify (a) /api/v1/version reports 0.54.0, (b) `fabt_platform_mfa_verify_total` is scrape-visible after a few real attempts, (c) `FabtPlatformMfaFailureSpike` + `FabtPlatformBackend5xx` rules loaded into Prometheus via /api/v1/rules, (d) Flyway HWM V90, (e) tree-shake correctness via container ls of dist/assets/. Rollback matrix documents 7 symptoms; V90 is one-way (forward-compatible additive functions) so backend rollback alone is sufficient — no DB rollback path.

## 8. Pre-merge

- [ ] 8.1 Warroom review of the implemented PR (security spot-check on JWT handling + Marcus's 5 print conditions verified in code)
- [ ] 8.2 Open PR against main; link this OpenSpec change; reviewer-approve required
- [ ] 8.3 Verify CI scans pass (CodeQL, npm audit, dependency-check)
- [ ] 8.4 Squash-merge to main; tag is deferred to v0.54 deploy day

## 9. Operator decision points (during /opsx:apply)

- [x] 9.1 `--color-platform` = **burnt orange `#C2410C`** (warm operator-mode feel; AA contrast 5.4:1 with white banner text; distinct from amber warning + DV purple). Light/dark variants TBD during 3.1.
- [x] 9.2 Operator email in banner: **masked** (e.g. `c***@gmail.com`). Defensive against shoulder-surfing / screenshot leakage.
- [x] 9.3 Backup-codes screen print button label: **"Print"** (modal's own buttons remain spec-locked to "Cancel" / "Print Anyway").

## 10. Known follow-ups for v0.55 backlog (warroom round-12)

These were flagged during the F11 review passes but accepted as not-blocking-v0.54-tag. File as v0.55 backlog before archiving this change:

- [ ] 10.1 **mfa-verify happy-path test gap.** The bookkeeping flip in §6.1 was honest about the rationale shape: no Playwright spec asserts `mfa-verify 200 → /platform/dashboard` end-to-end. The success branch is exercised inside the lockout-recovery test (`platform-ui-mfa-verify-errors.spec.ts:86-130`) but that test explicitly skips dashboard URL assertion. Add a 5-line success-path test to `platform-ui-routing.spec.ts` covering the normal mfa-verify-with-correct-code → dashboard transition.
- [ ] 10.2 **Help-link Playwright assertions.** Both `platform-dashboard-user-guide-link` and `platform-mfa-enroll-user-guide-link` testids exist in DOM but no spec asserts they exist with the correct `href` (the pinned `PLATFORM_OPERATOR_USER_GUIDE_URL` constant) and `target="_blank"`. Add two-line checks to `platform-ui-dashboard.spec.ts` and `platform-ui-routing.spec.ts` (against the mfa-enroll-route smoke).
- [ ] 10.3 **Unit test for `pendingAccessToken` deferred-login flow** (warroom round-12 newly-discovered #8). The capture spec catches a regression indirectly. A vitest spec asserting "after `/mfa-confirm` 200, `login()` is NOT called until BackupCodesDisplay onContinue fires" would harden the production-bug-fix at the unit layer.
- [ ] 10.4 **Slice-E destructive-flow re-screenshot.** When Slice E ships the destructive variant of ConfirmActionModal + the in-page POST handler, re-run `capture-platform-operator-screenshots.spec.ts` test 05 against the actual destructive variant and replace the user-guide caption.
- [ ] 10.5 **Round-9 #5 carry-over.** `isPlatformAuthFlowPath` allowlist in `frontend/src/pages/platform/helpers/platformApi.ts` is path-list-coupled to backend `PlatformAuthController` route mappings. A future endpoint rename silently breaks the allowlist (operators get logged out mid-form). Generate from OpenAPI or pin via a contract test before Slice E.
- [ ] 10.6 **Round-11 N6 carry-over.** Disabled-card visual signal: an operator scanning the dashboard sees 5 lifecycle cards with full-opacity chrome — distinguishable only by the `primaryDisabled` button color. Consider adding a "Disabled" badge or distinctive border-color via `aria-disabled` selector in v0.55. Bounded UX risk, NOT WCAG-blocking.
- [ ] 10.7 **Round-13 #4. `/mfa-confirm` parallel no-store header.** v0.54 added `Cache-Control: no-store, no-cache, must-revalidate, private` + `Pragma: no-cache` + `Expires: 0` to `/mfa-setup` (which carries the plaintext backup codes). The `/mfa-confirm` endpoint returns the post-MFA access token, which BFCache could in principle replay. Belt-and-braces: mirror the same header set onto `/mfa-confirm` and `/login/mfa-verify` (both return access tokens). Add backend ITs in PlatformAuthControllerMeTest.
- [ ] 10.8 **Round-13 #5. Print-codes spec test 5 rename.** `platform-print-codes.spec.ts:201` is titled "Cache-Control: no-store on /mfa-setup response (live backend assertion)" but only verifies endpoint existence + 401 status (the live backend isn't reachable for the success-path header without the JWT signing secret; the actual header assertion lives in `PlatformAuthControllerMeTest`). Rename to "/mfa-setup endpoint reachable (header asserted in backend IT)".
- [ ] 10.9 **Round-13 #6. Tab-close-during-codes-display Playwright coverage.** Round-12 raised the case "operator closes the tab during the codes phase — the post-MFA token in pendingAccessToken is discarded; backup codes were stored as hashes server-side; operator simply lost the plaintext." No spec exercises this path. Add a test to `platform-mfa-enroll.spec.ts` that closes the page mid-codes-phase and asserts the operator can still log in (mfa is enrolled), AND the backup-code count is still 10 (no consumption). Pairs with v0.55 #10.3 (unit test for pendingAccessToken).
- [ ] 10.10 **Round-13 #7. Training-walkthrough mock-vs-prod disclaimer.** `platform-training-walkthrough.spec.ts` narrates "the mocked backend accepts it" without disclaiming that production rejects bad credentials. An operator running the rehearsal could form the wrong mental model. Add a one-line `narrate()` early in the spec: "NOTE: this rehearsal mocks the backend — production rejects bad credentials with a 401."
- [ ] 10.11 **Round-13 #8. MFA compatibility doc detail-level rewrite.** `docs/operations/platform-operator-mfa-compatibility.md:23-25` mentions SHA-1 vs SHA-256 in operator-facing copy. Rewrite as operator-readable ("file an issue with the app + OS combination"); move the technical detail to `TotpService.java` JSDoc.
- [ ] 10.12 **Round-13 #11 (Riley). MFA compatibility CI lint gate.** Add a CI markdown check: fail the build if `docs/operations/platform-operator-mfa-compatibility.md` still contains > 4 `TBD` cells past the v0.55 tag. Prevents the file from staying all-TBD indefinitely.
